/// Spacing constants for C1 Hsch, as defined in design spec §2.4.
abstract final class AppSpacing {
  /// Base grid unit
  static const double unit = 8.0;

  /// Screen horizontal padding
  static const double screenH = 16.0;

  /// Card internal padding (horizontal)
  static const double cardH = 16.0;

  /// Card internal padding (vertical)
  static const double cardV = 12.0;

  /// Row gap in lists
  static const double rowGap = 8.0;

  /// Section gap between blocks
  static const double sectionGap = 16.0;

  /// Minimum tap target dimension
  static const double minTapTarget = 44.0;
}
